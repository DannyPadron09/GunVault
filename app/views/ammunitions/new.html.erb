<%= form_for :ammunition do |f| %>
    <div class="form-group">
        <%= f.label :caliber %>
        <%= f.text_field :caliber, class: "form-control" %>
    </div>

    <div class="form-group">
        <%= f.hidden_field :owner_ids %>
    </div>

    <div class="actions">
        <%= f.submit "Add New Ammo" %>
    </div>
<% end %>

