<h1>Caliber : <%= @ammo.caliber %></h1>
<h2>Quantity : <%= @ammo.quantity %></h2>

<% if current_user.id == @owner.id %>
    <%= link_to "My Vault", owner_path(@owner) %>
    <%= link_to "Edit Ammo Count", edit_ammo_path %>
    <%= link_to "Log Out", logout_path %>
<% end %>